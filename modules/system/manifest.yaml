name: system
description: Hostname, keyboard layout, GPU groups, firewall, bluetooth, tuned
version: 1.0.0
order: 10
condition: always
requires:
provides:
  - hostname
  - keyboard
  - gpu-groups
  - firewall
  - bluetooth
  - tuned
