name: shell
description: Shell environment, bash prompt, Fish shell, Kitty terminal, tmux
version: 2.0.0
order: 30
condition: always
requires:
  - packages
provides:
  - bootstrap-env
  - qt-env-vars
  - bash-config
  - fish-config
  - terminal-config
